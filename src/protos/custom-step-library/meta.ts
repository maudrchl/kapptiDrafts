import type { ProtoMeta } from '../registry'

const meta: ProtoMeta = {
  title: 'Custom step library',
  status: 'wip dev',
  updatedAt: '2026-07-24',
  description:
    'Custom coded steps saved to a shared library: import them into any test, edit once and it propagates everywhere',
}

export default meta
