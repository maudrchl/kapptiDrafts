import type { ProtoMeta } from '../registry'
import { IconFlag } from '@kapptivate/ui-kit'

const meta: ProtoMeta = {
  title: 'Variables: guided walkthrough',
  status: 'wip design',
  // `tag: 'PM'` = onglet PM de l'index, pour les supports de présentation
  tag: 'PM',
  icon: IconFlag,
  updatedAt: '2026-08-17',
  description:
    'Pas à pas sur l\'expérience « in-test vs local » pour la passation aux devs : le même écran que le proto, avec la décision derrière chaque zone',
}

export default meta
