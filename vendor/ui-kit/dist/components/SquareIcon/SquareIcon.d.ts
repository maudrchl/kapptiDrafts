import { ReactNode } from 'react';
type SquareIconProps = {
    icon: ReactNode;
    backgroundColor?: string;
    borderColor?: string;
};
declare const SquareIcon: ({ icon, backgroundColor, borderColor }: SquareIconProps) => JSX.Element;
export default SquareIcon;
