import * as React from 'react';
type CounterCardsProps = {
    children: React.ReactNode;
};
declare const CounterCardGroup: {
    ({ children }: CounterCardsProps): JSX.Element;
    Separator: ({}: {}) => JSX.Element;
};
export default CounterCardGroup;
