import { ReactNode } from 'react';
import { CollapseProps as AntdCollapseProps } from 'antd';
import { Size } from 'utils';
type CollapseProps = {
    items: CollapseItem[];
    headerClass?: string;
    classNames?: string;
    expandIcon?: ReactNode;
    expandIconColor?: string;
    expandIconPosition?: AntdCollapseProps['expandIconPosition'];
    defaultActiveKey?: string[];
    ghost?: boolean;
    size?: Size;
    collapsible?: AntdCollapseProps['collapsible'];
    noContentPadding?: boolean;
    noBorder?: boolean;
    onChange?: (key: string | string[]) => void;
    activeKeys?: string[];
};
type CollapseItem = {
    key: string;
    label: ReactNode;
    description?: ReactNode;
    open?: boolean;
    disabled?: boolean;
    children: ReactNode;
    extra?: (panelOpen: boolean) => ReactNode;
};
declare const Collapse: ({ items, headerClass, classNames, expandIcon, expandIconColor, expandIconPosition, defaultActiveKey, ghost, size, collapsible, noContentPadding, noBorder, onChange, activeKeys, }: CollapseProps) => JSX.Element;
export default Collapse;
