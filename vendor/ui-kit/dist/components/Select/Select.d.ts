import { ReactNode } from 'react';
import { Size } from 'utils';
import { Size as TSize } from 'components/Text/Text';
export declare const DEFAULT_LABEL_SIZE = "sm";
export declare const DEFAULT_SIZE = "m";
export declare const DEFAULT_MIN_WIDTH = "150px";
export declare const DEFAULT_SEARCHABLE = false;
export declare const DEFAULT_FULL_WIDTH = false;
export declare const DEFAULT_WITH_DIFFERENT_FULL_LABEL = false;
export declare const DEFAULT_DISABLED = false;
export type Option = {
    label: ReactNode;
    value?: any;
    description?: ReactNode;
    options?: Option[];
    [key: string]: string | number | boolean | ReactNode;
};
type OptionRender<T = Option> = Pick<Option, 'label' | 'value'> & {
    data: T;
};
export type SelectMode = 'multiple' | 'tags';
type SelectProps = {
    className?: string;
    popupClassName?: string;
    label?: string;
    labelSize?: TSize;
    size?: Size;
    options?: Option[];
    defaultValue?: any;
    searchable?: boolean;
    mode?: SelectMode;
    placeholder?: string | ReactNode;
    width?: string;
    minWidth?: string;
    maxWidth?: string;
    maxCount?: number;
    maxTagCount?: number | 'responsive';
    value?: any;
    onChange?: (e: any, value: any) => void;
    icon?: any;
    clearable?: boolean;
    onClear?: () => void;
    fullWidth?: boolean;
    withDifferentFullLabel?: boolean;
    invalid?: boolean;
    disabled?: boolean;
    renderDropdown?: any;
    open?: boolean;
    onToggleDropdown?: (open: boolean) => void;
    optionRender?: (option: OptionRender<any>) => ReactNode;
    optionFilterProp?: string;
    searchBarInDropdown?: boolean;
    antdThemeOverride?: Record<string, any>;
    showOptionDescription?: boolean;
    borderless?: boolean;
};
declare const Select: ({ className, popupClassName, label, labelSize, size, options, defaultValue, searchable, mode, placeholder, width, maxWidth, minWidth, maxCount, maxTagCount, value, onChange, icon: Icon, clearable, onClear, fullWidth, withDifferentFullLabel, invalid, disabled, renderDropdown, onToggleDropdown, open, optionRender, optionFilterProp: customOptionFilterProp, searchBarInDropdown, antdThemeOverride: themeOverride, showOptionDescription, borderless, }: SelectProps) => JSX.Element;
export default Select;
